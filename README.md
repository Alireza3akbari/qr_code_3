# QR Code Generator for Multiple Contacts
This Python script generates QR codes for multiple contacts at the same time. 
Each QR code is saved in the 'qrcodes' folder with a safe filename.

## How to use
1. Install required library: `pip install qrcode`
2. Run the script: `python qr_code_3.py`
3. Enter contact information as prompted.
